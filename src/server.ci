mainmodule server
{
    extern module libcharmtyles;
    PUPable sqrt_t;
    
    mainchare Main
    {
        entry Main(CkArgMsg*);

        entry void handle_command(int epoch, uint8_t kind, uint32_t size, char cmd[size]);
    };
}
